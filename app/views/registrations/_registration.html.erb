<%
  title @page.title
  meta_description @page.meta
%>

<% content_for :body_id do %> id="registration"<% end %>
<% content_for :body_class do %> <% end %>

<div role="tabpanel" id="lsetting">
  <h1><%= @page.title %></h1>
  <%= copy @page.copy %>
  <div class="col-xs-12">
    <%= render @form %>
  </div>
</div>