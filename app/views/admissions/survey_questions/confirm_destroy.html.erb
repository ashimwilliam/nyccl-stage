<% title "Delete #{@survey_question.question_title}"%>

<%= form_for :survey_template, url: admin_survey_question_path(@survey_question), html: { method: :delete } do |f| %>
    <h3>Are you sure you want to permanently delete -<%= @survey_question.question_title %></h3>
    <p>
      <button type="submit">Delete</button> or <%= link_to "Cancel", admin_survey_questions_path %>
    </p>
<% end %>