<% title "Delete #{@organization.name}"%>

<%= form_for :organization, url: admin_organization_path(@organization), html: { method: :delete } do |f| %>
  <h3>Are you sure you want to permanently destroy <%= @organization.name %>?</h3>
  <p>
    <button type="submit">Destroy</button> or <%= link_to "cancel", admin_organizations_path %>
  </p>
<% end %>