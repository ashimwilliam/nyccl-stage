<% title "Confirm Destroy " + @page.title %>

<%= form_for :page, url: admin_page_path(@page), html: { method: :delete } do |f| %>
  <h3>Are you sure you want to permanently destroy <%= @page.title %>?</h3>
  <p>
    <%= button_tag "Destroy" %> or <%= link_to "cancel", admin_pages_path %>
  </p>
<% end %>