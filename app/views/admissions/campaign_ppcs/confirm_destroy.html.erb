<% title "Delete #{@campaign_ppc.title}"%>

<%= form_for :campaign_ppc, url: admin_campaign_ppc_path(@campaign_ppc), html: { method: :delete } do |f| %>
    <h3>Are you sure you want to permanently destroy <%= @campaign_ppc.title %>?</h3>
    <p>
      <button type="submit">Destroy</button> or <%= link_to "cancel", admin_campaign_ppcs_path %>
    </p>
<% end %>