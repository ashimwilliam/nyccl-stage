c
@keyword_str
BlogPost.where(["title like ? or tagline like ? or body like ?", "%#{@keyword_str}%", "%#{@keyword_str}%", "%#{@keyword_str}%"]).order("updated_at DESC")
keyword_str
newstr
newstr = 'ashim'
BlogPost.where(["title like ? or tagline like ? or body like ?", "%#{@keyword_str}%", "%#{@keyword_str}%", "%#{@keyword_str}%"]).order("updated_at DESC")
keyword_str = params[:q]
keword_str
@keyword_str = params[:q]
keword_str
keyword_str = params[:q]
params
c
Resource.where(["name like ? or body like ? or teaser like ?", "%#{@keyword_str}%", "%#{@keyword_str}%", "%#{@keyword_str}%"]).order("updated_at DESC")
ForumThread.where(["name like ? or message like ? ", "%#{@keyword_str}%", "%#{@keyword_str}%"]).order("updated_at DESC")
Faq.where(["question like ? or answer like ?", "%#{@keyword_str}%", "%#{@keyword_str}%"]).order("updated_at DESC")
Event.where(["name like ? or body like ?", "%#{@keyword_str}%", "%#{@keyword_str}%"]).order("updated_at DESC")
BlogPost.where(["title like ? or tagline like ? or body like ?", "%#{@keyword_str}%", "%#{@keyword_str}%", "%#{@keyword_str}%"]).order("updated_at DESC")
keyword_str
keyword_str = params[:q]
keyword_str
params[:q].strip.split if params[:q]
c
Resource.where(["name like ? or body like ? or teaser like ?", "%#{params[:q]}%", "%#{params[:q]}%", "%#{params[:q]}%"]).order("updated_at DESC")
Scholarship.where(["name like ? or copy like ? or logged_in_copy like ? or meta_description like ?", "%#{params[:q]}%", "%#{params[:q]}%", "%#{params[:q]}%", "%#{params[:q]}%"]).order("updated_at DESC")
Faq.where(["question like ? or answer like ?", "%#{params[:q]}%", "%#{params[:q]}%"]).order("updated_at DESC")
Event.where(["name like ? or body like ?", "%#{params[:q]}%", "%#{params[:q]}%"]).order("updated_at DESC")
params[:q]
ForumThread.where(["name like ? or message like ? ", "%#{params[:q]}%", "%#{params[:q]}%"]).order("updated_at DESC")
BlogPost.where(["title like ? or tagline like ? or body like ?", "%#{params[:q]}%", "%#{params[:q]}%", "%#{params[:q]}%"]).order("updated_at DESC")
params
params[:q]
c
params
c
params
@params
c
@params
